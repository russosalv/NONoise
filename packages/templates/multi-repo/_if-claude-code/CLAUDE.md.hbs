# {{projectName}} — Claude Code context

Claude Code-specific conventions for this project. For cross-tool context see `AGENTS.md`.

## Workspace-centric skills

This is a NONoise multi-repo workspace. The `.claude/skills/` tree lives at
the workspace root (this directory). All skills — including Polly — are
available when you open this directory in Claude Code. Sub-repos under
`repos/` are managed as separate code repositories; they do NOT carry
their own skill tree. If you need skills inside a specific sub-repo,
copy the `.claude/` folder manually into that sub-repo (rarely needed).

## Entry point — Polly

This project ships with **Polly**, the NONoise SDLC advisor. When the developer is unsure
which skill to use, invoke Polly:

- Type `/polly` to launch
- Or say "start polly" / "avvia polly" / "orchestrate this project"

## Working with skills

This project ships a curated set of skills under `.claude/skills/`. When the user describes a task, your default path is to **engage a skill directly** based on the intent — do not route through Polly unless the user is explicitly lost ("where do I start?", "what's next?", "sono perso"). Examples: "I need to reverse engineer this codebase" → engage `reverse-engineering`; "draft requirements for X" → engage `bmad-agent-analyst`; "sprint breakdown for the ADR" → engage `sprint-manifest`.

**When you engage any SDLC skill, your first action is to ask the user for the full scope in one go — who, what, why, constraints, success criteria.** Use the skill's internal structure as a schema to fill, not as a turn-by-turn questionnaire. Only ask follow-up questions for genuine blockers (missing critical information you cannot infer). This applies to every SDLC skill — native, bundled, or vendored (superpowers/impeccable/skill-creator).

If the user is unsure which skill to use, they can invoke `/polly` (Claude Code), say "avvia polly" / "start polly" (Copilot and others), or you can offer Polly proactively as a fallback — never as the default.

## Installed skills

**SDLC orchestrator** — `polly`

**Requirements & discovery** — `requirements-ingest`, `bmad-agent-analyst`,
`bmad-advanced-elicitation`

**Architecture & validation** — `arch-brainstorm`, `arch-decision`, `quint-fpf`,
`bmad-agent-architect`

**Sprint & implementation** — `sprint-manifest`, `atr`

**Brownfield** — `reverse-engineering`

**Ops & observability** — `ops-skill-builder`, `observability-debug`

**Integrations** — `spec-to-workitem`, `playwright-cli`, `frontend-design`,
`bmad-agent-ux-designer`, `bmad-agent-tech-writer`

**Generators** — `vscode-config-generator`, `docs-md-generator`, `design-md-generator`

**Utility** — `skill-finder`

**Superpowers (vendored from `obra/superpowers`)** — `superpowers:brainstorming`,
`superpowers:writing-plans`, `superpowers:executing-plans`,
`superpowers:test-driven-development`, `superpowers:verification-before-completion`,
`superpowers:systematic-debugging`, `superpowers:subagent-driven-development`,
`superpowers:dispatching-parallel-agents`, `superpowers:using-git-worktrees`,
`superpowers:finishing-a-development-branch`, `superpowers:requesting-code-review`,
`superpowers:receiving-code-review`, `superpowers:writing-skills`,
`superpowers:using-superpowers`

Bundled skills live at `.claude/skills/<name>/SKILL.md`. Vendored superpowers
live at `.claude/skills/superpowers/<name>/SKILL.md`. Read the frontmatter for
the full trigger list. Slash commands for superpowers live in
`.claude/commands/superpowers/`.

## Working in this project

- Prefer editing existing files over creating new ones.
- Run tests before committing.
- Follow the project's code style.

{{#if user}}<!-- >>> user (managed by create-nonoise) -->
## user

{{#if user.name}}The developer's name is **{{user.name}}** — address them by name when it feels natural.
{{/if}}{{#if user.locale}}Reply in **{{user.localeLabel}}** (`{{user.locale}}`) by default, unless the user switches language mid-conversation.
{{/if}}<!-- <<< user -->

{{/if}}<!-- >>> polly (managed by polly skill) -->
## polly

Invoke Polly when the user types `/polly`, says "start polly" / "avvia polly" /
"run polly" / "orchestrate this project", or when they seem lost ("where do I start?",
"which skill for X?", "what's next?"). Polly is an advisor — suggest the right skill
when you're unsure, not the default entry point for every task.
<!-- <<< polly -->

<!-- >>> graphify (managed by create-nonoise) -->
## graphify

This project has a graphify knowledge graph at `graphify-out/`.

Rules:
- Before answering architecture or codebase questions, read `graphify-out/GRAPH_REPORT.md` for god nodes and community structure.
- If `graphify-out/wiki/index.md` exists, navigate it instead of reading raw files.
- For cross-module "how does X relate to Y" questions, prefer `graphify query "<question>"`, `graphify path "<A>" "<B>"`, or `graphify explain "<concept>"` over grep — these traverse the graph's EXTRACTED + INFERRED edges instead of scanning files.
- After modifying code files in this session, run `graphify update .` to keep the graph current (AST-only, no API cost).
<!-- <<< graphify -->

---

Generated by create-nonoise v{{frameworkVersion}}.
